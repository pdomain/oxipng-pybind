# Changelog

## Unreleased

- Added the planned `optimize_from_memory()` API for `bytes`, `bytearray`, and
  `memoryview` inputs.
- Expanded file optimization options and added Python enum helpers for the
  supported option surface.
- Enabled ABI3 wheel metadata and added local wheel tag checks.
- Added the artifact-only wheel workflow scaffolding.
- Added the upstream surface manifest and scanner scaffolding.
